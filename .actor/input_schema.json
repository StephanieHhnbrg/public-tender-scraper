{
    "title": "Input schema",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "keyword": {
            "title": "Keyword",
            "type": "string",
            "description": "Search term to look for in tender platforms",
            "default": "Printing",
            "editor": "textfield"
        },
        "maxResults": {
            "title": "Amount of results to retrieve",
            "type": "string",
            "description": "How many records to fetch per search: 10, 25, 50, 100, or * for all results.",
            "enum": ["10", "25", "50", "100", "*"],
            "default": "25"
        },
        "countries": {
            "title": "Countries",
            "type": "string",
            "description": "Which national tender platforms to be scrapped",
            "default": "GER",
            "editor": "textfield"
        },
        "groqApiKey": {
            "title": "Groq API Key",
            "type": "string",
            "description": "Enables translation of keywords to the platform language and conversion of platform data into the user's language.",
            "editor": "textfield",
            "isSecret": true
        }
    },
    "required": ["keyword"]
}
